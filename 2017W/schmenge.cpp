#include <iostream>
using namespace std;

enum way{
	open, left, right, up, down
};

struct connection{
	unsigned char speed;
	way direction;
};

int main(){
	unsigned char height;
	unsigned char width;
	cin >> height;
	cin >> width;
	while (height != 0 && width != 0){
		connection vline[20];
		connection hline[20];
		for ()
	}

	return 0;
}
